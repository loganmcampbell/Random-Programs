
CREATE DATABASE students_db;
USE student_db;

show databases;
use students_db;
show tables;
describe tables;

CREATE TABLE Students
(
	FirstName varchar(255),
	LastName varchar(255),
	SID varchar(255),
	Major varchar(255)
);
